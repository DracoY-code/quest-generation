# Contains the top-level packages installed via `pip install` or `uv pip install`.
# All other dependencies are managed by Micromamba through environment.yml.
# 
# To install these pip dependencies:
#   [uv ]pip install -r requirements.txt
# 
# To lock the environment:
#   [uv ]pip compile -r requirements.txt

torch==2.6.0
